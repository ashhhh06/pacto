{
  "name": "pacto-enterprise",
  "version": "2.0.0",
  "private": true,
  "description": "Pacto — AI-Powered Enterprise Contract Intelligence & Business Decision Platform",
  "scripts": {
    "start": "cd server && node index.js",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "google-auth-library": "^9.15.0",
    "mongoose": "^8.9.5"
  }
}
